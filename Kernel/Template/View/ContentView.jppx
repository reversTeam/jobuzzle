#import ('./MasterView') = MasterView;

class ContentView extends MasterView {

	render : function () {
		if (!this._template.getVisibility())
			return null;
		return this.display();
	}

}

#export ContentView;